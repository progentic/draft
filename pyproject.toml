[project]
name = "draft-helpers"
version = "0.1.0"
description = "Constrained helper-worker surface for DRAFT"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
