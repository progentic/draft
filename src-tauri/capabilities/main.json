{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-event-listener",
  "description": "Allows the main DRAFT window to receive typed Rust-owned events.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
